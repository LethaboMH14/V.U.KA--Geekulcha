## 2026-09-24 | Codex assistant at Sibusiso's request | Codex / GPT-6 | P3.S14/S17 CI and dependency-security gates | locally implemented; PR verification pending

**Research** — Read the CI task packet and the existing `.github/workflows/checks.yml`, root and `shared/` package manifests and lockfile, `docs/OVERLAPS.md`, `docs/build-log/README.md` and template, and the repository's security and evidence rules. Checked the Semgrep 1.177.0 release and documented `p/security-audit` ruleset choice; checked OSV-Scanner's v2 `scan source --recursive` usage and pinned the v2.6.0 amd64 container digest. Confirmed the setup-node v4.4.0 and setup-python v5.6.0 release SHAs from their official release pages.

**Real data / references** — FACT, local verification on 24 Sep: root Node suite **13 passed, 0 failed**; `shared/` Vitest suite **49 passed, 4 skipped, 0 failed**; root and shared `npm audit` reported **0 vulnerabilities**. A disposable fixture with `lodash@4.17.20` produced **1 high severity vulnerability** and `npm audit --audit-level=low` exited **1**. The fixture was removed and no vulnerable dependency was added to the repository. Tool references: [Semgrep 1.177.0 release](https://github.com/semgrep/semgrep/releases/tag/v1.177.0), [Semgrep security-audit ruleset](https://github.com/semgrep/skills), [OSV-Scanner v2.6.0 release](https://github.com/google/osv-scanner/releases/tag/v2.6.0), [OSV-Scanner source scan usage](https://google.github.io/osv-scanner/usage/), [setup-node v4.4.0](https://github.com/actions/setup-node/releases/tag/v4.4.0), [setup-python v5.6.0](https://github.com/actions/setup-python/releases/tag/v5.6.0).

**Business reasoning** — Reproducible tests and dependency gates lower the chance of a preventable security regression reaching the demo or a pilot, reducing review and incident costs without changing user-facing behavior.

**Competitor reference** — Not applicable; this is internal build and supply-chain hygiene, not a product capability or competitor claim.

Changed: `.github/workflows/checks.yml` now runs root `node --test`, `shared/` Vitest and Python tests when present; npm audits both locked Node projects; runs pip-audit when ANCHOR/server requirements manifests exist; runs pinned Semgrep 1.177.0 with `p/security-audit`; and scans source with digest-pinned OSV-Scanner v2.6.0. Existing actions remain commit-pinned. Added a root npm lockfile for reproducible `npm ci`, and `docs/security/SCA-WAIVERS.md` records a time-bounded, reviewer-owned waiver format without scanner-wide ignores. `team/sibusiso.md` records the CI/security path claim and the pending Ipeleng review. No application, contract or ADR changed.

Evidence: `node --test "test/**/*.test.mjs"` — 13 passed, 0 failed. In `shared/`, `npm test` — 49 passed, 4 skipped, 0 failed; `npm audit` — found 0 vulnerabilities. Root `npm audit` — found 0 vulnerabilities. Disposable negative check: `npm audit --audit-level=low --prefix <temp-fixture>` against `lodash@4.17.20` — exit 1 with one high-severity finding; the fixture was deleted. `node scripts/check-docs.mjs` — passed (human fact/quality review still required). `node scripts/check-intake.mjs` — passed with the repository's existing reviewer-authenticity reminder. `node scripts/test-security.mjs .tools/gitleaks.exe` — passed all isolated pre-commit success and refusal scenarios. `.tools/gitleaks.exe dir --redact --config .gitleaks.toml .` — no leaks; `.tools/gitleaks.exe git --redact --config .gitleaks.toml --log-opts="--all"` — 169 commits scanned, no leaks. `git diff --check` — exit 0. No `anchor/tests/` or `server/tests/` Python test files or `anchor/` / `server/` requirements manifests exist on this base, so pytest and pip-audit are configured to report and skip their current not-applicable state. Semgrep and OSV-Scanner were not run locally: the Semgrep install stalled on Windows during dependency resolution; Docker was installed but its daemon was unavailable. GitHub Actions acceptance was not run because this branch was not pushed and remote fetch failed (`Permission denied (publickey)` over SSH; HTTPS failed with `SEC_E_NO_CREDENTIALS`). A green PR run is still required; this entry does not claim it.

Decision: None. This work adds gates only and makes no product, contract or ADR decision.

Needs/blockers: Ipeleng, CI/security co-owner, to review the workflow and waiver policy. Sibusiso to run the workflow on a PR and record the actual remote result. No remote branch or PR was created by this session.

Business handoff: not applicable; no user-facing or deployable capability changed. The waiver template tells future dependency owners what evidence must accompany any exception.

Next: Sibusiso and Ipeleng review the exact branch head; run the workflow on a PR to establish Semgrep/OSV and Linux runner results before marking P3.S14/S17 complete.
