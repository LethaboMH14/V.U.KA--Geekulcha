## 2026-09-26 | Sibusiso, with Claude Code assistant | Implementation | run_workers now drives bank_signal and anchor batching, not just guardian_alert | PROPOSED

**Research** — Sibusiso asked for the backend strengthened end to end, specifically so Lethabo (or anyone) can pull the repo, run it, and have Hedera and the live Azure deployment actually work for a real transaction, not just the API. Traced why nothing anchored or reached the bank outside tests: `server/run_workers.py` (Lethabo, 25 Sep) only ever claimed `guardian_alert` outbox rows by design ("bank signals and anchor requests are left for their own workers"), but no such workers were ever started anywhere — not in a doc, not in `startup.sh`. `server/bank_worker.py` (`deliver_bank_signal`) and `server/anchoring.py` (`BatchCoordinator`, a full Hedera-testnet batch coordinator with its own 60 s coalescing and advisory lock) already existed, fully built and tested, just never invoked outside their own test files.

**Real data / references** — `server/outbox.py`'s `claim_due(..., kinds=...)` already supports a multi-kind claim (added earlier today for the guardian/anchor starvation fix on #99); `anchor/hedera-sidecar/cli.mjs`'s `--sim-stub` mode was checked and found to always return exit code 2 ("positively not submitted") — it is a failure-path test fixture, not a safe way to fabricate a "confirmed" receipt, so it was deliberately not used as a default local-dev stub. This codebase never fabricates a ledger receipt (see `anchoring.py`'s own comments); a shortcut stub would have contradicted that.

**Business reasoning** — Consolidating all three effects into the one process Lethabo already runs is lower operational cost than teaching her to run three separate worker binaries, and it is what `run_workers.py`'s own original docstring already promised ("one process that runs the background work"). The anchor coordinator runs unconditionally, using the real transport always: with no Hedera testnet credentials configured, batches are created from real chain heads but simply stay `submitted`/unconfirmed, which is the honest state this system is built to represent, not an error to route around.

**Competitor reference** — Not applicable.

Decision (**PROPOSED**): `server/run_workers.py` claims `("guardian_alert", "bank_signal")` and delivers each by kind; a `BatchCoordinator` (real `publish_root`/`reconcile_mirror_root`) ticks once per second alongside it. `VUKA_SIM_BANK_URL` (default `http://127.0.0.1:8001`) points bank_signal delivery at a locally run `sim_bank`. `startup.sh` now also launches `python -m server.run_workers &` before `exec`-ing uvicorn, gated by `VUKA_SKIP_WORKERS`, since the outbox lease and the coordinator's advisory lock are already safe under concurrent workers. `scripts/build-deploy-package.py` now packages `anchor/hedera-sidecar/*.mjs`/`*.json` (not `node_modules`, not its own tests) so the sidecar's source at least ships — `npm ci --ignore-scripts` there is still a deploy-target's own responsibility, same as its README always said.

Evidence (FACT, real PostgreSQL): full suite 298/298 before this change (baseline); `server/tests/test_run_workers_all_effects.py` (new) drives one `step()` tick against a live sim_bank over a real socket and a fixture Hedera receipt, and proves a guardian_alert, a bank_signal, and an anchor_request/batch all complete in that single tick — reverting the `kinds` filter back to guardian-alert-only was confirmed to fail this test (`delivered == 1` instead of `2`) before restoring the fix. `scripts/tests/test_build_deploy_package.py`: new `test_hedera_sidecar_source_is_packaged_but_not_node_modules_or_its_tests`; the pre-existing "excludes sidecars" test was renamed and its assertion inverted to match, since the sidecar is now intentionally included. Locally ran `bash startup.sh` end to end against real PostgreSQL (a throwaway `vuka_startup_smoke` database): both processes came up, `/healthz` returned 200, `run_workers` logged no exceptions across multiple ticks.

Decision: None accepted.

Needs/blockers: whether Azure App Service's Python container has a Node runtime available for the sidecar (`anchor/publish.py` shells out to `node`) is unverified — checking the live container hit a production-read restriction in this session's tooling, so it is left as an open question for Sibusiso rather than guessed at. Without Node there, anchor batches will keep being created and staying unconfirmed on Azure even with real Hedera credentials configured, the same as they do today with none.

Business handoff: Not applicable.

Next: confirm Node availability on the live App Service (or add it), then set real `HEDERA_OPERATOR_ID`/`HEDERA_OPERATOR_KEY`/`HEDERA_SUBMIT_KEY`/`HEDERA_MANIFEST_SEQUENCE` for a real anchored demo transaction.
