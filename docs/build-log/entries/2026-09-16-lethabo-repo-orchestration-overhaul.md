## 2026-09-16 | Lethabo (Claude Sonnet 5 session) | claude-sonnet-5 | repo process overhaul | authored, self-review recorded

**Research** — Read the actual conflict history from the same afternoon: five PRs (#35, #34, #33, #30, #27) merged in ~25 minutes, four hit a real git conflict, every one on `docs/BUILD-LOG.md` at the exact same append point. Web research (16 Sep) on how real teams solve reviewer-assignment and shared-log contention: GitHub's own CODEOWNERS docs (docs.github.com), a practitioner writeup on its silent-failure modes (devtoolhub.com, arnica.io), the official `claude-code-action` GitHub Marketplace listing and Anthropic's own docs (code.claude.com/docs/en/github-actions), and an independently-built third-party Claude Code skill (`multi-agent-coordination-skill`, brattri3/DimaMuhannad on GitHub) that converged on the same role-file + append-only-log shape this repo already uses — checked as validation, not copied. Full citations in `docs/AGENT-PIPELINE.md`.

**Real data / references** — The conflict count (4 of 5 PRs) and the exact files that collided (`docs/BUILD-LOG.md`, `docs/CHECKLIST.md`, `team/khutso.md`, `team/ipeleng.md`) are from this session's own PR-merge work minutes earlier, not estimated. CODEOWNERS' silent-failure behaviour is a direct quote from the cited source, not paraphrased from memory.

**Business reasoning** — Every hour spent resolving an avoidable merge conflict is an hour not spent on the product. Seven people shipping in parallel for eleven more days means this compounds; fixing the structural cause once is cheaper than re-fixing the same conflict shape on every future concurrent PR.

**Competitor reference** — Not applicable; this is internal process, not a product feature.

Changed: `docs/BUILD-LOG.md` frozen (banner added, historical content untouched) — new entries go in `docs/build-log/entries/`, one file per entry (`docs/build-log/README.md`, `docs/build-log/TEMPLATE.md`, this file as the first real entry). Added `.github/CODEOWNERS` mapping the existing reviewer assignments (RULES.md's "Lethabo first-reviews Mutarisi/Ipeleng/Babatunde; Sibusiso first-reviews Vukosi/Khutso") to GitHub's native auto-request-reviewer feature. Added `docs/AGENT-ROUTING.md` (tool-agnostic execute/review/repo-gate principle — explicitly not a mandate to use any particular model or tool; personal tooling stays personal). Added `docs/AGENT-PIPELINE.md` (research findings, cited, plus what's real but not yet installed: branch protection enforcing CODEOWNERS is `G17`, blocked on repo visibility; the official Claude Code GitHub Action needs an interactive `/install-github-app` OAuth step only the repo owner can do; third-party skill marketplaces are named but deliberately not installed pending an actual read of what they'd execute). Edited `RULES.md` and `AGENTS.md`: a PR-comment finding that matters now must also become a written rule in the same or next PR, not stay in an archived thread; every build-log entry requires Research / Real data / Business reasoning fields (`AGENTS.md`'s existing UI-only quality contract's discipline, generalised to all work); all `docs/BUILD-LOG.md` references repointed to the new location. Updated `docs/SESSION-PROMPT.md`'s read-order and workflow diagram. Added a `docs/build-log/entries/` filename-format check to `scripts/check-docs.mjs`.

Evidence: `node scripts/check-docs.mjs` — run after this entry lands (see PR). `.github/CODEOWNERS` untested against a real PR yet — flagged explicitly in `docs/AGENT-PIPELINE.md` as needing one throwaway PR per owner to confirm the auto-request actually fires, since the researched failure mode is silent, not an error.

Decision: no contract, security or product-capability change. Process/governance only. Self-reviewed (author = both-lead account on this session); genuine second-person review from Sibusiso still needed before this is treated as team-accepted, same bar as any other RULES.md change.

Needs/blockers: CODEOWNERS needs the live-fire test above. Branch protection (`G17`) still blocked on repo visibility. `/install-github-app` needs Lethabo (or whoever holds repo admin) to run it interactively — not doable from an agent session.

Business handoff: not applicable — internal process, no household-facing capability changed.

Next: open PR, request Sibusiso's review, run the CODEOWNERS live-fire test once merged, decide on the Claude Code GitHub Action install separately.
