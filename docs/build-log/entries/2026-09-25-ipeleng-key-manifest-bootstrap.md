## 2026-09-25 | Ipeleng (verify page, key manifest), via her coding assistant | Claude Code assistant (Cline), disclosed per AGENTS.md D12 | P3.S4 key-manifest bootstrap + §10 serialization pin | PROPOSED — Lethabo's review pending; Sibusiso's server-min must adopt the same serialization

**Research** — Read `docs/VUKA-2-SPEC.md` §4 (format-v2 entry and signed statement), §5, §10 (key manifest, `0x02 ‖ SHA-256(key manifest)`) and §14 (verify-min scope), plus §15 T03/T04/T05 oracles in `docs/security/TEST-SPECS.md`. Checked PR #51 (`feat/sibusiso-contract-v2`) so the manifest lands in the shape contract v2 already documents: the `KeyManifest` schema is `{server_ed25519_public_key, ml_dsa_65_public_key?}`, `additionalProperties: false`, and `contracts/keys/README.md` on that branch leaves the manifest **serialization explicitly unpinned** — "implementations must not emit a fingerprint until that byte representation is pinned". The pinned topic id comes from Sibusiso's 23 Sep Hedera spike entry: `0.0.10687280`, testnet.

**Real data / references** — `FACT`, checked 25 Sep: `cd shared && npx vitest run` → **64 passed, 0 failed, 5 skipped** (the skips are T01/T02 awaiting Sibusiso's vector files; the 5 new manifest tests and 10 new verify tests all pass). Fingerprint of the committed manifest over its canonical JSON bytes: `c1d90404edd180765e15321d690881933c5a4ffcdecc18f064131b6c5c70a0a2`; epoch message `02c1d904…a0a2` (33 bytes). The server Ed25519 keypair was generated locally with Node `crypto.generateKeyPairSync('ed25519')`; the SPKI is 44 bytes (12-byte header + 32-byte raw) and imports through WebCrypto as an Ed25519 verify key.

**Business reasoning** — The verify page must reject any export whose server signatures do not verify against a key the team pinned, not a key the export itself carries (T05). Bootstrapping the manifest now also pins the exact bytes that get hashed, which unblocks Sibusiso's server-min (deploy due 09:00) to publish the same `0x02` message before the first batch, per §10.

**Competitor reference** — Not applicable.

Changed: new files only; nothing existing was rewritten except `shared/README.md` (two module lines added).
- `contracts/keys/manifest.json` — the pinned server Ed25519 **public** SPKI, exactly the contract-v2 `KeyManifest` shape (ML-DSA omitted until built). No private key material; the private half was generated into a scratch file **outside** the repo and must be handed to Sibusiso before the server-min deploy — never committed, and rotated before any use beyond the testnet demo.
- `contracts/keys/verify-pins.json` — what the verify page pins: network `testnet`, topic `0.0.10687280` (the spike's topic), `topic_epoch: 1`, the manifest fingerprint and the serialization name.
- `shared/keys.js` — canonical-manifest bytes, the 33-byte `0x02` message, anchor-message decode, SPKI importers (Ed25519 and P-256 ECDSA) and a `validateManifest` mirroring the contract-v2 schema.
- `shared/verify.js` — the verify-min export verifier: structure, prev links, commitment recompute, `event_hash` recompute, §4 statement signatures (device/guardian keys from registration `signer_pubkey`; server key only from the pinned manifest) and the §4/§7 counter rule; reports the first broken entry index (T04 oracle). Mirror/proof checks stay out of scope until the mirror is readable.
- `shared/test/fixtures.js`, `shared/test/manifest.test.js`, `shared/test/verify-export.test.js` — a canned 8-entry sim-subject export (genesis registration → duress check-in → server triage → `bank_signal_sent`) plus negative vectors: altered `ts`/payload/`prev_hash` at index 3, a substituted device key (re-signed genesis; dependent entry fails), server entries re-signed with a foreign Ed25519 key (fails at the first server entry), a substituted pinned manifest key and a receipt naming a foreign topic.
- `shared/README.md` — module lines for `keys.js` and `verify.js`.

Evidence: `cd shared && npm test` → **64 passed, 0 failed, 5 skipped (T01/T02 pending vectors)**; the manifest fingerprint was recomputed from the committed file bytes inside the test and matches `contracts/keys/verify-pins.json`. Gitleaks note: only a public key is committed; the private half never entered the repo.

Decision: pins the §10 manifest serialization the contract left open — **manifest bytes = canonical JSON per §5 (`shared/canonical.js`), fingerprint = SHA-256 of those bytes, epoch message = `0x02 ‖ fingerprint`**. This is the team-consistency decision Sibusiso's `contracts/keys/README.md` asked for; his server must emit the same fingerprint. Needs both leads' sign-off (this PR's review + Sibusiso's adoption), since §10 said not to emit fingerprints until pinned.

Needs/blockers: **Sibusiso** — receive the server signing key out-of-band before the 09:00 deploy, have server-min publish the `0x02` message before the first batch, and make the export shape match contract v2 (`SubjectExport` with `entries`/`payloads`/`salts`). **Vukosi** — T03 device-signature fixture still pending. **PR #74** — this work imports nothing from it yet but stays stacked on it; rebase when it merges.

Business handoff: the verify-min page (next step, before 10:00) reads `contracts/keys/manifest.json` + `contracts/keys/verify-pins.json` directly, so what a buyer sees pinned is exactly what the repo committed.

Next: Ipeleng — verify-min page + vitest (T03/T04) before 10:00, wire to Sibusiso's deployed server-min export and record the first roundtrip before 12:00; PR #57 acceptance follow-up; rebase after #74 merges. Full verify page + mirror checks are Saturday.