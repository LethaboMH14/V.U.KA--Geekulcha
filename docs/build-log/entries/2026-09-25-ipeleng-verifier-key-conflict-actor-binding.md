## 2026-09-25 | Ipeleng (verifier rebase + security fix), via her coding assistant | Claude Code assistant (Cline), disclosed per AGENTS.md D12 | PR #79 review response — rebased on #69 (landed via main), added the pinned-server-key layer, SEC-A/B adversarial tests | IN PROGRESS — pushed for fresh security + lead review

**Research** — Lethabo's REQUEST_CHANGES on PR #79 (red-team findings SEC-A/SEC-B, seconded by Sibusiso's corrective comment), plus #69's implementation which **merged to main** (review option 2 became reality before the fold could). Per the review's stated path for that case: "#69 lands first and #79 rebases to add the manifest and server-signature checks". The rebase takes main's `shared/verify.js` (with its `key_conflict`, `actor_mismatch`, `assurance` and canonical-base64/DER rules — all already tested there) as the base and adds what #79 uniquely contributes: the pins layer (T05) and the §10 manifest bootstrap.

**Real data / references** — `FACT`, checked 25 Sep: `cd shared && npx vitest run` → **94 passed, 0 failed, 5 skipped**. The merged verifier passes **all 25 of #69's existing `verify.test.js` cases unchanged** (pins are optional; without them the verifier is byte-for-byte #69's behaviour) plus 15 tests in the new `verify-export.test.js` (8 T04 + 4 SEC-A/B review + 3 T05) and the manifest suite.

Changed: `shared/verify.js` (rebased onto main's, then the pins layer added), `shared/README.md` (keys.js + verify.js lines), plus new files `shared/keys.js`, `contracts/keys/*`, `shared/test/fixtures.js`, `shared/test/manifest.test.js`, `shared/test/verify-export.test.js` and two build-log entries.
- **Pins layer (T05, #79's unique contribution)** — `verifyExport(exportObj, pins?)`: with pins, each server-signed entry's §4 statement verifies against `pins.server_ed25519_public_key` (Ed25519, from the pinned `contracts/keys/manifest.json` — never from the export); failure is `server_signature_invalid` at that entry, and `not_checked` drops "server signatures" (`server_signature_not_checked` empties). Receipts in the export must name `pins.topic_id`/`pins.topic_epoch` (`receipt_topic_mismatch`). Without pins, #69's contract holds exactly: server indexes are recorded as unchecked.
- **SEC-A `key_conflict`** — already canonical in main's #69 version (register-once; a re-registration with a different key or actor fails). This PR keeps it and proves it from #79's fixtures: a forged mid-chain entry re-registering `device_verifymin_01` with the attacker's key fails at index 2, while an identical re-registration still passes.
- **SEC-B `actor_mismatch`** — already canonical in main's #69 version. Proven here with a re-signed + re-hashed entry claiming the guardian's actor id so only the binding can catch it (T21 style).
- **`assurance: "internal_consistency_only"`** — carried by every result, as #69 specified.

Evidence: the suite above; each negative test asserts both `first_broken_index` and the rule name in `reason`. `node scripts/check-docs.mjs` and `node scripts/check-intake.mjs` pass at this head.

Decision: the verify-min API is now ONE canonical verifier — #69's shape (`entries_checked`, `reason`, `not_checked`, `assurance`) with optional pins. The dashboard passes pins from `contracts/keys/verify-pins.json` + `manifest.json`; cross-language scripts keep calling it without pins. `chainHeadHex` is exported for the page (head → `0x01` root message). Flagged for fresh review: server entries carry no per-actor binding (the manifest pins one global server key; the anchor's actor is a single constant) — if the red-team wants per-actor server binding, it is a small follow-up.

Needs/blockers: fresh security + lead review on #79 (Lethabo to dismiss the stale REQUEST_CHANGES; Sibusiso to formally supersede his qualified approval, noting his Codex packet's findings are now covered by main's verifier + these tests — no separate worktree push needed); then #80 refreshes on top of this head. #74 has merged (24 Sep 17:36), so this rebase also clears the stacked-PR conflicts.

Next: rebase `feat/ipeleng-verify-min` (#80) onto this head, wire verify-min to Sibusiso's deployed server-min export and record the roundtrip before 12:00; PR #57 acceptance follow-up. Full verify page + mirror checks are Saturday.