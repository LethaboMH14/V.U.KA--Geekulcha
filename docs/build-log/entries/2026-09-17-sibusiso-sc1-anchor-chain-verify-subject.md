## 2026-09-17 | Sibusiso (Claude Sonnet 5 session, ClaudeX-Loop execute pass) | claude-sonnet-5 | SC.1 anchor/chain+verify+subject | in progress, review pending

**Research** — Read `contracts/openapi.yaml`'s frozen `EvidenceEntry`, `IntegrityResult`, `SubjectRecord` schemas directly (all `additionalProperties: false` — exact field sets matter). Read `server/src/auth/governance.py` for the existing code convention (dataclasses, pure functions, no I/O, docstring-first) and matched it. Checked `server/` for an existing dependency file or FastAPI scaffold before writing any route code — found neither exists yet, so did not introduce an unpinned `fastapi` dependency; built a framework-agnostic handler instead, with a docstring showing the exact one-line FastAPI wiring for whenever that scaffold lands.

**Real data / references** — Every schema field name, requiredness, and nullability in `anchor/chain.py`/`verify.py`/`subject.py` is checked against the live `contracts/openapi.yaml`, not a memory of it: a permanent test (`server/tests/test_contract_schema_validation.py`) loads the actual YAML file and validates real handler output against it with `jsonschema.Draft202012Validator` (same tool PR #33 used to re-verify ADR-0030's shapes) — including a rejection case, confirming `additionalProperties: false` actually has teeth in this test rather than passing vacuously. The `sim_` fixture chain reuses the exact Musa scenario already canonical in `docs/ANCHOR-RATIONALE.md` Scenario 3, the Figma "My Record" screen, and `docs/MASTER-CONTEXT.md`'s own showcase description — same actor (`op_A41`), same reason ("recognised delivery route"), same timestamps, so a demo walkthrough is consistent across every artefact a judge might open.

**Business reasoning** — This is the literal showcase `docs/MASTER-CONTEXT.md` names as the single most important thing to demonstrate — it's the C2/C3/C4 answer in one moment. Every day this stays unbuilt is a day the team's strongest differentiator has nothing behind it but a diagram.

**Competitor reference** — This is exactly the capability `docs/ANCHOR-RATIONALE.md` Scenario 3 and `docs/COMPETITORS.md` say no commercial camera network (Vumacam named specifically) offers: a subject can request their own decision history. Nobody else in this category hands a person their file.

Changed: new `anchor/chain.py` (prev-hash chain, canonical `event_hash = SHA256(json.dumps(entry, sort_keys=True))`), `anchor/verify.py` (first-broken-link-by-index, never a boolean — tested against both a tampered-content case and a spliced-prev_hash case, and a case with two breaks confirming only the first is reported), `anchor/subject.py` (SubjectRecord assembly, with an explicit `sim_`-labelled honesty boundary on the anchor state since `publish.py` doesn't exist yet). New `server/src/api/subjects.py` — framework-agnostic `GET /v1/subjects/{id}/record` handler (401/404/200 paths), plus the canonical Musa `sim_` fixture chain. New `requirements-dev.txt` — pins `pytest`, `jsonschema`, `PyYAML`, closing a real pre-existing gap (pytest was already used in `brain/tests/` with no dependency file recorded anywhere, per earlier BUILD-LOG entries noting a clean clone couldn't run it). Updated `anchor/README.md` with an honest per-file status table and the same honesty-boundary statement. Updated `docs/CHECKLIST.md` SC.1 to ◐ with the precise true/not-true split.

Evidence: `python3 -m pytest anchor/tests/ server/tests/ -v` → **25 passed, 0 failed**. Includes three tests that load the real `contracts/openapi.yaml` and validate real handler output against it with `jsonschema.Draft202012Validator`, not a hand-copied description of the schema. `node scripts/check-docs.mjs` — see PR.

Decision: no contract change — implements against the already-frozen contract exactly, does not propose a change to it. Deliberately did not add a `fastapi` dependency without server/'s own scaffold decision; deliberately did not fabricate a working public-anchor claim.

Needs/blockers: **the showcase is not complete.** Still needed, all outside this leaf's scope: `anchor/publish.py` (OpenTimestamps submission — the actual public-anchor half of SC.1's own acceptance criterion), a real persistence layer (`server/src/db/` is still `.gitkeep`-only), a real FastAPI app scaffold and auth module to attach this handler to. These are named explicitly in `anchor/README.md` and this checklist row so nobody presents this PR's output as a finished showcase.

Business handoff: not applicable yet — no household-facing capability exists until the above lands; this is the tested logic it will be built on.

Next: `anchor/publish.py` is the highest-value follow-up — it's the one piece that turns this from "a tested hash chain" into "the actual showcase." Sibusiso's call on sequencing against the server scaffold work.
