## 2026-09-23 | Lethabo (co-lead) | AI coding assistant (disclosed per ADR-0038) | Coercion scenario catalogue, CEM-0; feeds ADR-0039 | PROPOSED — for Lethabo, Ipeleng and Vukosi to review

**Research** — Read `docs/VUKA-2-SPEC.md` §1–§3 and §7–§10 (duress signal, deadlines, incidents, PIN authority, anchoring), §15–§17, `docs/STAGED-DURESS-DEFENCE.md`, ADR-0036/0037/0038 and the honesty ledger in `docs/MASTER-CONTEXT.md`. Implemented the CEM-0 model specification (evidence tally in decibans, hard rules H1–H8, fast path) exactly. Checked every YAMNet label and index used against `yamnet_class_map.csv` from the tensorflow/models repository (521 classes; sha256 recorded in the generated doc). "Crying, sobbing" is index 19, and "Tire squeal" is spelled that way in the CSV. Used a sourced research brief for South African coercion context. Its "not independently fetched" markers are kept in the doc's sources section.

**Real data / references** — ISS Africa (3 Dec 2024) on express kidnapping and hijacking-linked kidnapping; TechCentral (7 Nov 2023) on forced app transfers and ATM runs; Scientific American (4 Aug 2017) on tonic immobility; ACLU of Massachusetts (8 Apr 2024) on acoustic gunshot-alert false positives. The search-engine-only facts (house-robbery transfer, staged kidnapping, fireworks days, SIM swap and vishing shares) are labelled as such. The research found no source for night or month-end timing, so those weights are held at 0.

**Business reasoning** — A bank or insurer partner will first ask what VIGIL does in the case they care about. A deterministic, checkable catalogue that also names what VIGIL cannot see answers that before the pitch, and shows the gaps before a judge finds them.

**Competitor reference** — Not applicable. No competitor behaviour is claimed.

Changed (all new files):
- `scripts/coercion_scenarios.py` — a stdlib-only, integer-only, deterministic generator. It holds the REASONS table, the hard rules as code, K, the bands, the fast path, the base-rate illustration and 133 scenarios in categories A–O. It asserts H2: no detection-only scenario passes T1.
- `docs/COERCION-SCENARIOS.md` — generated by the script, never hand-edited. It covers the model, the YAMNet classes, the reasons, the hard rules, every scenario with its reasons, total, K, band, tier and rule, fast path, E-level and recorded kinds, the base-rate illustration, 19 "what we forgot" items and the sources.
- `test/coercion-scenarios.test.mjs` — runs `--check`, asserts ≥ 110 scenarios and the category minimums, and checks that no scenario reaches T2 or T3 from detection alone. It skips if no Python 3.11+ is found.

Evidence: `python scripts/coercion_scenarios.py --check` passed under Python 3.11 and 3.14, and the output was identical across four `PYTHONHASHSEED` values. `node scripts/check-docs.mjs` passed. `node --test "test/**/*.test.mjs"`: 13 passed, 0 failed (3 of them new).

Decision: None taken. This is input to ADR-0039, which is still to be written. The top open items for Lethabo:
- a check-in that is never shown has no `no_answer` deadline (I01);
- a false alarm answered with a normal PIN never closes, so a later dead zone reaches the bank signal (P3.L8, review blocker 3);
- ending a journey is not PIN-gated (F06).

Needs/blockers: Lethabo decides the fast path and the three items above. Ipeleng reviews H4, P3.L8 and the bank-trigger provenance. Vukosi reviews the YAMNet mapping and the motion look-back window. The M1, M2 and M7 measurements are needed before any weight is called calibrated.

Business handoff: Not applicable. No figures changed.

Next: Lethabo drafts ADR-0039 from this catalogue on Thu 24 Sep, alongside P3.L8 (due Thu 24 12:00).
