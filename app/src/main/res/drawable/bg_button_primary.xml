<?xml version="1.0" encoding="utf-8"?>
<!-- Primary pill. The ripple gives press and focus feedback clipped to the
     pill; without it Android draws a grey rectangular focus highlight. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#33FFFFFF">
    <item>
        <shape android:shape="rectangle">
            <!-- angle 90 runs bottom → top: #2A4A73 at the top, #1A3354 at the bottom -->
            <gradient
                android:angle="90"
                android:endColor="@color/vuka_action_grad_top"
                android:startColor="@color/vuka_action_grad_bottom" />
            <corners android:radius="27dp" />
        </shape>
    </item>
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="27dp" />
        </shape>
    </item>
</ripple>
