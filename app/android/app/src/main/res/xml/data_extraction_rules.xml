<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+: exclude everything from cloud backup AND device-to-device transfer.
     allowBackup="false" alone does not stop D2D transfer on API 31+. VIGIL keeps keys,
     the PIN hash and the event queue on the device; none of it may migrate (T42). -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
